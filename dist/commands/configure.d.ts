/**
 * Asistente interactivo: OpenAI, Ollama, Gemini (OpenAI-compatible) o Claude (Messages API).
 * Termina con la misma prueba que `llm-probe`.
 */
export declare function runInteractiveConfig(root: string): Promise<void>;
